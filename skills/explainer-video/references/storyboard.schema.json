{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "explainer-video-storyboard",
  "title": "Explainer video storyboard",
  "type": "object",
  "required": ["title", "style", "voice", "characters", "scenes"],
  "properties": {
    "title": { "type": "string", "description": "Video title (used for output naming)" },
    "aspect_ratio": { "enum": ["16:9", "9:16", "1:1"], "default": "16:9" },
    "style": {
      "type": "string",
      "description": "Global style string appended to every image prompt (look, line quality, palette, lighting)"
    },
    "voice": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": { "type": "string", "description": "ElevenLabs voice name, e.g. Brian, Rachel, Daniel" },
        "speed": { "type": "number", "minimum": 0.7, "maximum": 1.2, "default": 1.0 },
        "stability": { "type": "number", "minimum": 0, "maximum": 1, "default": 0.5 }
      }
    },
    "music": {
      "type": "object",
      "description": "Optional background music bed mixed under the narration",
      "properties": {
        "file": { "type": "string", "description": "Local path to a music file" },
        "volume": { "type": "number", "minimum": 0, "maximum": 1, "default": 0.12 }
      }
    },
    "characters": {
      "type": "array",
      "description": "Recurring characters; each gets one reference image reused across scenes",
      "items": {
        "type": "object",
        "required": ["id", "prompt"],
        "properties": {
          "id": { "type": "string", "description": "Short id referenced by scenes, e.g. 'A'" },
          "prompt": { "type": "string", "description": "Reference-sheet prompt (appearance, outfit, age) — style string is appended automatically" }
        }
      }
    },
    "scenes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "vo", "prompt"],
        "properties": {
          "id": { "type": "integer", "description": "Scene number (ordering)" },
          "label": { "type": "string", "description": "Optional act/beat label, e.g. 'Hook', 'Mechanism'" },
          "vo": { "type": "string", "description": "Narration line for this scene. Keep under ~35 words (≈14s spoken)" },
          "prompt": { "type": "string", "description": "Still-image prompt — style string and character refs are added automatically" },
          "characters": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Character ids appearing in this scene (their reference images are passed to the generator)"
          },
          "motion": { "type": "string", "description": "Camera/animation direction for the clip, e.g. 'slow lateral tracking'" },
          "still": {
            "type": "boolean",
            "default": false,
            "description": "If true, this scene is NOT sent to the video model. The still image is given a Ken Burns move locally with ffmpeg instead — free. Use for talking-point scenes; reserve animation for beats where motion carries the joke or the action."
          },
          "still_motion": {
            "enum": ["zoom_in", "zoom_out", "pan_left", "pan_right", "pan_up", "static"],
            "default": "zoom_in",
            "description": "Ken Burns move for still scenes. Vary it across consecutive stills so the video doesn't feel mechanical."
          },
          "min_seconds": { "type": "number", "minimum": 3, "maximum": 15, "default": 4, "description": "Minimum scene length even if narration is shorter" }
        }
      }
    }
  }
}
